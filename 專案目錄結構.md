# 專案目錄結構（更新：2026-03-09）

> 目的：同步目前後端實際結構，並標示每個核心檔案用途。  
> 範圍：列出核心目錄與關鍵檔案，不含 `target/`、IDE 快取與暫存輸出。

---

## 根目錄

```text
Brainwave/
|-- pom.xml                                   # Maven 父工程（聚合三個模組與共用依賴版本）
|-- README.md                                 # 後端專案說明與文件索引（入口）
|-- 專案目錄結構.md                            # 本檔：目錄與檔案用途索引
|-- Dockerfile                                # 後端容器建置腳本（多模組）
|-- .dockerignore                             # Docker 建置忽略清單
|-- scripts/
|   `-- seed-users-members.sql                # users/members 測試資料種子 SQL
|-- .github/
|   |-- README.md                             # CI/CD 流程補充說明
|   `-- workflows/
|       |-- ci-backend.yml                    # 後端 CI（編譯/測試）
|       `-- cd.yml                            # 後端 CD（部署流程）
|-- docs/                                     # 說明文件（中文檔名）
|   |-- 架構與請求流程.md                       # 請求流程圖、三層模組、啟動與稽核時序
|   |-- 後端部署指南.md                         # 部署（本地/Zeabur）
|   |-- 終端機操作指令.md                       # 編譯、測試、格式化
|   |-- 認證與Token說明.md                     # 認證與 Token（Mock / JWT）
|   |-- 權限守衛與RBAC說明.md                  # 權限與路徑設定
|   |-- 稽核日誌說明.md                        # @Audit、sink=console/db
|   |-- 檔案儲存說明.md                        # Storage 抽象（Local / S3）
|   |-- 系統設定API說明.md                     # 系統設定 Key-Value
|   |-- OpenAPI與契約說明.md                   # OpenAPI / Swagger / 前端型別
|   |-- 設定治理說明.md                        # Config Layer（app.*）
|   |-- 請求追蹤說明.md                        # Correlation ID / X-Request-ID
|   |-- 資料匯出說明.md                        # CSV 匯出
|   |-- 統一回應格式說明.md                    # Result / ResultMapper
|   |-- 轉換器說明.md                          # MapStruct / BaseConverter
|   |-- 身分模組模板.md                        # Identity 模組模板與整併
|   |-- API版本化策略.md                       # API 版本策略
|   |-- 安全與限流說明.md                      # 安全標頭與限流指引
|   `-- 資料庫遷移說明.md                      # Flyway V1/V2/V3
|-- brainwave-core/                           # 共用核心模組（基礎設施）
|-- brainwave-service/                        # 業務服務模組（領域邏輯）
`-- brainwave-backend/                        # API 啟動模組（Controller/啟動）
```

---

## 模組結構

### 1) `brainwave-backend`（API 與啟動層）

```text
brainwave-backend/
|-- pom.xml                                   # backend 模組依賴（含 Flyway）
`-- src/main/
    |-- java/com/brainwave/backend/
    |   |-- BrainwaveBackendApplication.java  # Spring Boot 啟動入口
    |   |-- auth/
    |   |   |-- AuthController.java           # 後台登入 API：/api/auth/login
    |   |   |-- AuthFrontController.java      # 前台登入 API：/api/front/auth/login
    |   |   |-- guard/AuthGuardInterceptor.java  # 路徑守衛（依 scope/role 保護 API）
    |   |   |-- token/MockTokenVerifier.java  # Mock Token 驗證（預設，@Profile("!jwt")）
    |   |   |-- token/JwtTokenVerifier.java  # JWT 驗證（@Profile("jwt")）
    |   |   `-- dto/
    |   |       |-- AuthLoginRequest.java     # 登入請求 DTO
    |   |       `-- AuthLoginResponse.java    # 登入回應 DTO（token + user）
    |   |-- config/
    |   |   |-- DefaultResultMapper.java      # 預設 Result 組裝（可替換為企業格式）
    |   |   `-- WebMvcConfig.java             # 註冊 AuthGuard 等 Interceptor
    |   |-- filter/
    |   |   `-- CorrelationIdFilter.java      # 注入 X-Request-ID、寫入 MDC 供日誌追蹤
    |   |-- health/
    |   |   `-- HealthController.java         # 健康檢查 API：/api/health
    |   |-- user/
    |   |   `-- UserController.java           # 使用者 CRUD/查詢 API：/api/users
    |   |-- member/
    |   |   `-- MemberController.java         # 會員 CRUD/查詢 API：/api/members
    |   `-- systemconfig/
    |       `-- SystemConfigController.java   # 系統設定 Key-Value API：/api/system-configs
    `-- resources/
        |-- application.properties             # 共用預設設定（JPA/Flyway/CORS/Auth）
        |-- application-local.properties       # 本地環境覆寫設定（local DB）
        |-- application-prod.properties        # 正式環境覆寫設定（prod）
        |-- test_data_users.sql                # 使用者測試資料 SQL
        |-- META-INF/additional-spring-configuration-metadata.json # 自訂 app.* 設定鍵 metadata
        `-- db/migration/
            |-- V1__init_users_members.sql     # 基礎表：users, members
            |-- V2__audit_log.sql              # 稽核日誌表
            `-- V3__system_config.sql          # 系統設定表
```

### 2) `brainwave-service`（業務邏輯層）

```text
brainwave-service/
|-- pom.xml
`-- src/main/java/com/brainwave/service/
    |-- BrainwaveServiceConfiguration.java
    |-- user/          (entity, repository, service, converter, dto, vo, request)
    |-- member/        (同上結構)
    |-- systemconfig/  (同上結構)
    `-- identity/template/  (Entity/DTO/VO/Request/Repository/Converter/Service 模板)
```

### 3) `brainwave-core`（共用基礎設施層）

```text
brainwave-core/
`-- src/main/java/com/brainwave/core/
    |-- base/      (BaseEntity, BaseRepository, BaseService, BaseServiceImpl)
    |-- common/    (BaseController, Result, ResultMapper, PageResponse)
    |-- converter/ (BaseConverter)
    |-- config/    (CorsConfig, JpaAuditingConfig, properties/*)
    |-- audit/     (Audit, AuditAspect, AuditLogService, Console/Db 實作, Entity, Repository)
    |-- export/    (CsvExportHelper)
    |-- exception/ (BaseException, BusinessException, GlobalExceptionHandler, ErrorResponse)
    `-- utils/     (PasswordUtil)
```

---

## 設定與部署現況

- Profiles：`local`、`prod`
- DB migration：Flyway，路徑 `brainwave-backend/src/main/resources/db/migration`（見 **docs/資料庫遷移說明.md**）
- 容器部署：根目錄 `Dockerfile` + `.dockerignore`
- 測試資料：`scripts/seed-users-members.sql`、`brainwave-backend/.../test_data_users.sql`

---

## API 與認證現況

- 後台登入：`POST /api/auth/login`
- 前台登入：`POST /api/front/auth/login`
- 健康檢查：`GET /api/health`
- 管理 API：`/api/users`、`/api/members`、`/api/system-configs`
- 認證：預設 Mock Token；可改為 JWT（profile=jwt、app.auth.jwt.secret），詳見 **docs/認證與Token說明.md**。
